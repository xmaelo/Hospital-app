let session;
let extension;
export default class x {
 
 setSession = (sess) => {
 		session = sess;
 }

 getSession = () => {
 		return session;
 }

 setEX = (ex) => {
 		extension = ex;
 }

 getEx = () => extension;

}